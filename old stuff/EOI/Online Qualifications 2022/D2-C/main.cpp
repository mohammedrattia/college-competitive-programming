#include <iostream>
#include<utility>
#include <vector>
//return dfs(adj[node][a%(int)adj[node].size()],a/(int)adj[node].size(),node)

using namespace std;
typedef long long ll;

int arr[1000005];

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    ll n, a, b, q;
    cin >> n >> q;
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i]
    }
    int l, r;
    cin >> l >> r;
    l--, r--;
    int cnt = 0;
    for (int i = l; i <= r; i++)
    {
        if (arr[i] == 1)
        {
            if ( == r)
        }
    }
}

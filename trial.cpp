#include <bits/stdtr1c++.h>
#define dbg(x) cout << (#x) << " : " << x << endl
#define dbeg(x) cout << (#x) << " : " << x << ", "
using namespace std;

int n, m;
void solve()
{
  cin >> n >> m;
  dbg(n);
}

int main ()
{
  int t;
  cin >> t;
  while (t--)
  {
    solve();
  }
}
